# =============== Application Configuration ===============
spring.application.name=ourikaT

# =============== JWT Configuration ===============
    jwt.secret=your-256-bit-secret-key-base64-encoded
jwt.expiration=86400000

# =============== Database Configuration ===============
spring.datasource.url=jdbc:postgresql://localhost:5432/ourikatravels
spring.datasource.username=ouchin55edcx
spring.datasource.password=ouchin55edcx
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate/JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Disable schema validation if you encounter metadata issues (temporary fix)
# spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# =============== Security & Logging ===============
spring.security.debug=false

# Logging Configuration
logging.level.com.ouchin.ourikat=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=debug

# =============== Server Configuration ===============
server.port=8080
server.servlet.context-path=/api